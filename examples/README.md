# yaml examples

Runnable pure-Ruby usage of the `yaml` library (Psych-compatible), verified under the rbgo interpreter.

```sh
rbgo examples/yaml_usage.rb
```

| File | Shows |
| --- | --- |
| `yaml_usage.rb` | `YAML.dump` / `YAML.load` round-trip, `Object#to_yaml`, and `YAML.safe_load` |
